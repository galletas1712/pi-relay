[package]
name = "agent-rpc"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
agent-core = { path = "../agent-core" }
agent-session = { path = "../agent-session" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

