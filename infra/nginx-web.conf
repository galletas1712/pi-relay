server {
	listen 80;
	server_name _;
	root /usr/share/nginx/html;
	index index.html;

	# Workspace routes are client-side; fall back to the SPA shell.
	location / {
		try_files $uri $uri/ /index.html;
	}

	location = /index.html {
		add_header Cache-Control "no-store";
	}
}
