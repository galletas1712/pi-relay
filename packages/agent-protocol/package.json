{
  "name": "@pi-relay/agent-protocol",
  "version": "0.1.0",
  "description": "Shared protocol types for orchestrator and session core seams",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "tsgo -p tsconfig.build.json",
    "dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "shx": "^0.4.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
