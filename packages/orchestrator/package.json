{
	"name": "@pi-relay/orchestrator",
	"version": "0.1.0",
	"description": "Multi-agent orchestrator: lifecycle management, message routing, tool tracking",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json",
		"dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
		"test": "vitest --run"
	},
	"dependencies": {
		"@pi-relay/agent-core": "^0.67.5",
		"@pi-relay/agent-protocol": "^0.1.0",
		"@pi-relay/ai": "^0.67.5",
		"@pi-relay/coding-agent": "^0.67.5",
		"@pi-relay/orchestrator-core": "^0.1.0",
		"@sinclair/typebox": "^0.34.40"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
