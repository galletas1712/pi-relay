{
  "name": "@pi-relay/bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "M5: pi-relay bridge/supervisor \u2014 WS product process supervising pi --mode rpc session hosts (slim contract v0, PG control plane)",
  "scripts": {
    "start": "node src/index.ts",
    "migrate": "node src/migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@pi-relay/workspace-lib": "*",
    "pg": "^8.13.1",
    "smol-toml": "^1.7.1",
    "ws": "^8.18.0",
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "@types/ws": "^8.5.13",
    "typescript": "^5.5.0"
  }
}
