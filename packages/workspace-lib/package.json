{
	"name": "@pi-relay/workspace-lib",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "M8: port of pi-relay rust/crates/agent-runtime workspaces — per-session btrfs subvolumes, multi-dir workspaces, reflink copies/snapshots, confined browse",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "node --experimental-strip-types --test test/*.test.mjs"
	},
	"devDependencies": {
		"@types/node": "^24.0.0",
		"typescript": "^5.5.0"
	}
}
